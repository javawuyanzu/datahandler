export declare class StringHashMap<TValue> {
    private map;
    addItem(key: string, value: TValue): void;
    getItem(key: string): TValue;
    readonly count: number;
    each(func: (key: string, value: TValue) => void): void;
    containsKey(key: string): boolean;
    remove(key: string): void;
    clear(): void;
}
export declare class StringSet {
    private map;
    add(item: string): void;
    remove(item: string): void;
    has(item: string): boolean;
    clear(): void;
}
